<% title(@artist.name) -%>

<ul class="rounded">
  <li>
    <%= image_tag @artist.avatar(:medium), :width => 150 %>
    <%= simple_format(@artist.bio) %>
  </li>
</ul>
<ul class="rounded">
  <% @tracks.each do |t| -%>
    <li>
      <%= link_to h(t.name), download_track_path(t), :rel => "external" %>
    </li>
  <% end -%>
</ul>
