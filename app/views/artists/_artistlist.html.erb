<ul class="tracklist" id="artistlist">
<% @artists.each do |a| %>
  <li class="grid_3<%= cycle(' alpha', '', '', ' omega') %>">
    <h2><%= link_to a.name, artist_path(a) %></h2>
    <p><%= a.bio %></p>
  </li>
<% end %>
</ul>
