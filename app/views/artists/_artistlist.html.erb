<ul id="tracklist">
<% @artists.each do |a| %>
  <div class="artist grid_3">
    <li class="track">
      <h2><%= link_to a.name, artist_path(a) %></h2>
      <p><%= a.bio %></p>
    </li>
  </div>
<% end %>
</ul>

