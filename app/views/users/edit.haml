#dialog
  %h1 Edit my account
  %ul
    -form_for @user, :url => account_path do |f|
      =f.error_messages
      =render :partial => 'form', :object => f
      %li= f.submit 'Update'
    %br
    =link_to 'My Profile', account_path
